fix(wen): 修复对话出现孤立编号"1." + 强化贴合用户问题

问题：/wen 页面 AI 回复中频繁出现孤零零的"1."后跟单条建议，
且回复偏泛、未先回应用户具体问题。

根因：buildChatSystemPrompt 中"如需列表可使用 1./2./3."暗示
AI 用编号列表，但短回复只生成一项，就出现孤立的"1."。

修复 (src/lib/prompts.ts buildChatSystemPrompt):
- 强制纯散文体：禁用 1./2./-/*/• 等任何编号或项目符号
- 明确开头必须先回应用户在问什么/在意什么
- 多个点用"首先…其次…"/"一方面…另一方面…"自然衔接
- 结尾建议自然嵌入末句，不另起行、不加序号、不加"建议:"标签
- 长度从 "200 字以内" 调整为 "120-220 字" 保留可读性
- 中英版本同步修改
